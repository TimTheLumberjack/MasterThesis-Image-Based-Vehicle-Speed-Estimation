# Copilot instructions for this repo

## Project snapshot (current)
- Workspace currently contains a single file: ui.py (empty).
- No README or existing AI instruction files were found via the standard glob search.
- No other modules, packages, or configuration files are present to infer architecture or workflows.

## What to do when editing
- Treat this as a single-file Python project unless new files are added.
- If you add code, keep it self-contained in ui.py or create clear, purposeful modules as requested.

## Architecture & data flow (observed)
- None yet; the codebase is empty, so no component boundaries or data flow can be derived.

## Conventions & patterns (observed)
- None yet; no existing code or style to follow.

## Dependencies & integrations (observed)
- None declared or discoverable in the current workspace.

## Developer workflows (observed)
- No build/test/run instructions are present in this workspace.

## Files to check first
- ui.py (currently empty).

## Gaps to confirm with the user
- Intended purpose of the tool, UI framework, and runtime environment.
- Preferred project structure, dependencies, and execution commands.
- Any existing workflow expectations (tests, packaging, linting).
